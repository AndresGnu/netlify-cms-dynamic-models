{
  "private": true,
  "workspaces": {
    "packages": [
      "generator/",
      "components",
      "client/*"
    ]
  },
  "scripts": {
    "dev": "flow vite && concurrently -k -n Javascript,Styles....,Server.... -c yellow.bold,magenta.bold,blue.bold \"yarn javascript dev\" \"yarn styles dev\" \"yarn generator dev\"",
    "dev2": "flow vite && concurrently -k -n Javascript,Styles....,Server.... -c yellow.bold,magenta.bold,blue.bold \"yarn javascript dev\" \"yarn styles dev\" \"yarn generator dev2\"",
    "dev-client": "flow vite && concurrently -k -n Javascript,Styles.... -c yellow.bold,magenta.bold,blue.bold \"yarn javascript dev\" \"yarn styles dev\"",
    "generator": "yarn workspace @app/generator",
    "javascript": "yarn workspace @app/vue",
    "styles": "yarn workspace @app/styles",
    "lambda:build": "netlify-lambda build functions/src",
    "lambda:start": "netlify-lambda serve functions/src",
    "preview": "vite preview ./netlify --c netlify/preview"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^4.19.0",
    "@typescript-eslint/parser": "^4.19.0",
    "concurrently": "^6.0.0",
    "eslint": "~7.22.0",
    "jest": "^26.6.3",
    "lerna": "^4.0.0",
    "netlify-lambda": "^2.0.3",
    "ts-jest": "^26.5.4",
    "typescript": "^4.2.3",
    "stylelint-config-recommended": "^4.0.0",
    "pkg": "^4.5.0"
  },
  "dependencies": {
    "nodemailer": "^6.5.0"
  }
}
